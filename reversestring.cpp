#include <iostream>
#include <cstring>
using namespace std;

int main()
{
    char str[100];
    int len;

    cout<<"Enter string: ";
    cin.getline(str,100);

    len = strlen(str);

    while(str=)
}